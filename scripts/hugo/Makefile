NSMAKE = nsmake run
NSMAKE += -DPAREX_DIR=\"`pwd`\"
NSMAKE += -DSDOT_DIR=\"`pwd`\"
NSMAKE += -Isrc
NSMAKE += -g3

all:
	# ${NSMAKE} tests/test_parex.cpp
	${NSMAKE} tests/test_SetOfElementaryPolytops.cpp

